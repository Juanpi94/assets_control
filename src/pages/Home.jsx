import React from "react";

const Home = ({title}) => {
    return (
        <div className="flex flex-col">
        <h1 className="text-xl font-bold">{title} </h1>
        </div>
    );
    }
    export default Home;